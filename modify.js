const fs = require('fs');

// TODO:
// Copy icon file to assets folder.

// CREATE folder in inputs folder for the new icon ?

// TODO:
// Take config title, and modify pbiviz.json file, to affect build.
// Modify pbiviz.json file, by parsing JSON data, updating, then writing the prettified JSON data back to file.

//JSON.parse(obj)

//JSON.stringify(obj, null, 2);

//fs.writeFileSync